/** Moved to `src/shared/DialogShell.tsx` (feature-neutral, shared with
 *  cities-management). Kept as a re-export so cook-review and driver-review
 *  imports are unchanged. */
export { default } from '../shared/DialogShell'
